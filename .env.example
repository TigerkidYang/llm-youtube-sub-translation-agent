# OpenAI API Key - Replace with your actual key
OPENAI_API_KEY="YOUR_OPENAI_API_KEY_HERE"

# Directory where original and translated subtitle files will be saved.
# Default: transcripts
TRANSCRIPT_OUTPUT_DIR="transcripts"

# Number of subtitle entries to process in each chunk by the agent.
# Default: 50
AGENT_CHUNK_SIZE="50"

# Maximum number of retries for translating a single chunk if validation fails.
# Default: 2
AGENT_MAX_TRANSLATION_RETRIES="2"

# Maximum number of retries for fetching subtitles via YouTube API.
# Default: 20
YOUTUBE_API_MAX_RETRIES="20"

# Delay in seconds between retries for fetching subtitles via YouTube API.
# Default: 3
YOUTUBE_API_RETRY_DELAY_SECONDS="3"

# LLM model name for the subtitle extraction phase.
# Default: o3-mini
EXTRACTION_MODEL="o3-mini"

# LLM model name for the subtitle translation phase.
# Default: o3-mini
TRANSLATION_MODEL="o3-mini"
